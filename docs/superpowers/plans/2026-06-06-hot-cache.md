# Hot Cache Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 그래프 상태를 압축한 세션 진입 컨텍스트(`hot.md`)를 결정적으로 생성하고, vault 빌드 시 작성하며 `projectos_get_hot_context` MCP 도구로 노출한다.

**Architecture:** 순수 조립기 `compose_hot_context` + 마크다운 렌더러 `render_hot_markdown`를 신규 모듈 `app/services/hot_context.py`에 둔다. `ObsidianWriterAgent.run()`이 빌드 후 렌더 그래프를 재유도해 `hot.md`를 쓰고, MCP 도구가 `vault_reconcile._rendered_graph`로 동일 렌더 그래프를 얻어 즉석 조립한다.

**Tech Stack:** Python, NetworkX, 기존 `app/utils/graph_health.py`(`check_isolated_nodes`), `app/utils/graph_restructure.py`(demote/details) 재사용.

**참고:** spec은 `VaultPayload`에 `rendered_graph` 필드 추가를 제안했으나, `VaultPayload`는 pydantic `BaseModel`이라 `nx.DiGraph` 필드는 `arbitrary_types_allowed` 설정을 요구한다. 더 단순하게, `run()`이 보유한 원시 graph에서 `_write_hot`이 렌더 그래프를 재유도한다(demote+details 1회 추가 호출, O(graph), 허용 범위). `VaultPayload`는 변경하지 않는다.

**테스트 실행 경로:** 모든 pytest는 `/raid/home/a202121010/workspace/projects/ProjectOS/src/backend`에서 실행. git 명령은 repo 루트에서 실행. (작업 디렉터리가 git 후 루트로 되돌아가므로 절대경로 사용.)

---

### Task 1: compose_hot_context 조립기

**Files:**
- Create: `src/backend/app/services/hot_context.py`
- Test: `src/backend/tests/test_services/test_hot_context.py`

- [ ] **Step 1: Write the failing tests**

Create `src/backend/tests/test_services/test_hot_context.py`:

```python
import networkx as nx

from app.services.hot_context import compose_hot_context


def _graph() -> nx.DiGraph:
    g = nx.DiGraph()
    g.add_node("p1", type="Person", name="Alice", description="A researcher in ML and graphs.")
    g.add_node("p2", type="Person", name="Bob", description="Engineer")
    g.add_node("pr1", type="Project", name="ProjectOS")
    g.add_node("pr2", type="Project", name="MiroFish")
    g.add_node("s1", type="Skill", name="Python")
    g.add_node("s2", type="Skill", name="NetworkX")
    g.add_node("iso", type="Skill", name="Rust")
    g.add_node("cat", type="Category", name="Skills")
    # Alice is the top hub: 4 edges
    g.add_edge("p1", "pr1", relation="DEVELOPED")
    g.add_edge("p1", "pr2", relation="DEVELOPED")
    g.add_edge("p1", "s1", relation="USES_SKILL")
    g.add_edge("p1", "s2", relation="USES_SKILL")
    # Bob: 1 edge
    g.add_edge("p2", "pr1", relation="DEVELOPED")
    # ProjectOS gets extra edges to be top Project hub
    g.add_edge("pr1", "s1", relation="USES_SKILL")
    return g


def test_persona_top_person_by_degree():
    ctx = compose_hot_context(_graph(), project_id="proj", top_n=5)
    persona = ctx["persona"]
    assert persona[0]["name"] == "Alice"
    assert persona[0]["type"] == "Person"
    assert persona[0]["degree"] >= persona[1]["degree"]
    assert persona[0]["description"].startswith("A researcher")
    assert all(p["type"] == "Person" for p in persona)


def test_persona_respects_top_n():
    ctx = compose_hot_context(_graph(), top_n=1)
    assert len(ctx["persona"]) == 1


def test_hubs_by_type_excludes_person_and_category():
    ctx = compose_hot_context(_graph(), top_n=5)
    hubs = ctx["hubs_by_type"]
    assert "Person" not in hubs
    assert "Category" not in hubs
    assert hubs["Project"][0]["name"] == "ProjectOS"
    assert all("degree" in h for h in hubs["Project"])


def test_gaps_isolated_named_nodes_sorted():
    ctx = compose_hot_context(_graph(), top_n=5)
    names = [g["name"] for g in ctx["gaps"]]
    assert "Rust" in names
    assert names == sorted(names)
    # connected nodes never appear
    assert "Alice" not in names


def test_recent_activity_headers_only():
    log = [
        "# ProjectOS Log",
        "",
        "## 2026-06-01 graph build",
        "- Nodes: 5",
        "## 2026-06-05 graph delta",
        "- Nodes: 7",
    ]
    ctx = compose_hot_context(_graph(), recent_log=log, recent_n=5)
    assert ctx["recent_activity"] == [
        "## 2026-06-01 graph build",
        "## 2026-06-05 graph delta",
    ]


def test_recent_activity_respects_recent_n():
    log = [f"## 2026-06-0{i} graph build" for i in range(1, 6)]
    ctx = compose_hot_context(_graph(), recent_log=log, recent_n=2)
    assert ctx["recent_activity"] == [
        "## 2026-06-04 graph build",
        "## 2026-06-05 graph build",
    ]


def test_stats_excludes_category():
    ctx = compose_hot_context(_graph())
    stats = ctx["stats"]
    assert "Category" not in stats["by_type"]
    assert stats["by_type"]["Person"] == 2
    assert stats["by_type"]["Project"] == 2
    assert stats["total_nodes"] == 7  # 8 nodes minus 1 Category


def test_empty_graph():
    ctx = compose_hot_context(nx.DiGraph(), project_id="empty")
    assert ctx["persona"] == []
    assert ctx["hubs_by_type"] == {}
    assert ctx["gaps"] == []
    assert ctx["recent_activity"] == []
    assert ctx["stats"]["total_nodes"] == 0
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/test_services/test_hot_context.py -v`
Expected: FAIL with `ModuleNotFoundError: No module named 'app.services.hot_context'`

- [ ] **Step 3: Write minimal implementation**

Create `src/backend/app/services/hot_context.py`:

```python
import networkx as nx

from app.utils.graph_health import check_isolated_nodes


def _degree(graph: nx.DiGraph, node_id: str) -> int:
    return graph.degree(node_id)


def compose_hot_context(
    graph: nx.DiGraph,
    project_id: str | None = None,
    recent_log: list[str] | None = None,
    top_n: int = 5,
    recent_n: int = 5,
) -> dict:
    """Assemble a compact, deterministic session-entry context from a rendered graph.

    Input graph is assumed already rendered (demote + build_entity_details applied).
    """
    persons: list[tuple[int, str, str, str]] = []
    hubs_raw: dict[str, list[tuple[int, str]]] = {}
    by_type: dict[str, int] = {}
    page_nodes = 0

    for node_id, data in graph.nodes(data=True):
        ntype = data.get("type", "")
        name = data.get("name", "")
        if ntype == "Category" or not name:
            continue
        page_nodes += 1
        by_type[ntype] = by_type.get(ntype, 0) + 1
        deg = _degree(graph, node_id)
        if ntype == "Person":
            persons.append((deg, name, data.get("description", "") or "", ntype))
        else:
            hubs_raw.setdefault(ntype, []).append((deg, name))

    persons.sort(key=lambda x: (-x[0], x[1]))
    persona = [
        {"name": n, "type": t, "degree": d, "description": desc[:80]}
        for (d, n, desc, t) in persons[:top_n]
    ]

    hubs_by_type: dict[str, list[dict]] = {}
    for ntype, items in hubs_raw.items():
        items.sort(key=lambda x: (-x[0], x[1]))
        hubs_by_type[ntype] = [
            {"name": n, "degree": d} for (d, n) in items[:top_n]
        ]

    recent_activity: list[str] = []
    if recent_log:
        headers = [ln for ln in recent_log if ln.startswith("## ")]
        recent_activity = headers[-recent_n:]

    gaps = sorted(
        (
            {"name": iso["name"], "type": iso["type"]}
            for iso in check_isolated_nodes(graph)
            if iso.get("name")
        ),
        key=lambda x: x["name"],
    )[: top_n * 2]

    # total_edges among page (non-Category, named) nodes
    page_node_ids = {
        n for n, d in graph.nodes(data=True)
        if d.get("type") != "Category" and d.get("name")
    }
    total_edges = sum(
        1 for u, v in graph.edges()
        if u in page_node_ids and v in page_node_ids
    )

    return {
        "project_id": project_id,
        "persona": persona,
        "hubs_by_type": hubs_by_type,
        "recent_activity": recent_activity,
        "gaps": gaps,
        "stats": {
            "total_nodes": page_nodes,
            "total_edges": total_edges,
            "by_type": by_type,
        },
    }
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/test_services/test_hot_context.py -v`
Expected: PASS (8 tests)

- [ ] **Step 5: Commit**

```bash
cd /raid/home/a202121010/workspace/projects/ProjectOS && git add src/backend/app/services/hot_context.py src/backend/tests/test_services/test_hot_context.py && git commit -m "feat(hot): deterministic hot-context composer"
```

---

### Task 2: render_hot_markdown 렌더러

**Files:**
- Modify: `src/backend/app/services/hot_context.py`
- Test: `src/backend/tests/test_services/test_hot_context.py`

- [ ] **Step 1: Write the failing tests**

Append to `src/backend/tests/test_services/test_hot_context.py`:

```python
from app.services.hot_context import render_hot_markdown


def test_render_sections_and_wikilinks():
    ctx = compose_hot_context(_graph(), project_id="proj")
    md = render_hot_markdown(ctx)
    assert "# Hot Context — proj" in md
    assert "## 주요 인물" in md
    assert "## 핵심 엔티티" in md
    assert "## 공백" in md
    assert "## 요약" in md
    assert "[[Alice]]" in md
    assert "[[ProjectOS]]" in md
    assert "[[Rust]]" in md  # isolated node


def test_render_empty_sections_show_none():
    ctx = compose_hot_context(nx.DiGraph(), project_id="empty")
    md = render_hot_markdown(ctx)
    assert "## 주요 인물" in md
    assert "- (없음)" in md


def test_render_is_deterministic():
    ctx = compose_hot_context(_graph(), project_id="proj")
    assert render_hot_markdown(ctx) == render_hot_markdown(ctx)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/test_services/test_hot_context.py -k render -v`
Expected: FAIL with `ImportError: cannot import name 'render_hot_markdown'`

- [ ] **Step 3: Write minimal implementation**

Append to `src/backend/app/services/hot_context.py`:

```python
def render_hot_markdown(ctx: dict) -> str:
    pid = ctx.get("project_id") or "(unknown)"
    lines = [
        f"# Hot Context — {pid}",
        "",
        "_Auto-generated session primer. Do not edit manually._",
        "",
        "## 주요 인물",
    ]
    if ctx["persona"]:
        for p in ctx["persona"]:
            desc = f" — {p['description']}" if p["description"] else ""
            lines.append(f"- [[{p['name']}]] (deg {p['degree']}){desc}")
    else:
        lines.append("- (없음)")

    lines += ["", "## 핵심 엔티티"]
    if ctx["hubs_by_type"]:
        for ntype in sorted(ctx["hubs_by_type"]):
            lines.append(f"### {ntype}")
            for h in ctx["hubs_by_type"][ntype]:
                lines.append(f"- [[{h['name']}]] (deg {h['degree']})")
    else:
        lines.append("- (없음)")

    lines += ["", "## 최근 활동"]
    if ctx["recent_activity"]:
        for entry in ctx["recent_activity"]:
            lines.append(f"- {entry.lstrip('# ').strip()}")
    else:
        lines.append("- (없음)")

    lines += ["", "## 공백 (연결 보강 후보)"]
    if ctx["gaps"]:
        for g in ctx["gaps"]:
            lines.append(f"- [[{g['name']}]] ({g['type']})")
    else:
        lines.append("- (없음)")

    stats = ctx["stats"]
    by_type = ", ".join(
        f"{t}: {c}" for t, c in sorted(stats["by_type"].items())
    ) or "(없음)"
    lines += [
        "",
        "## 요약",
        f"- Nodes: {stats['total_nodes']}, Edges: {stats['total_edges']}",
        f"- {by_type}",
        "",
    ]
    return "\n".join(lines)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/test_services/test_hot_context.py -v`
Expected: PASS (11 tests)

- [ ] **Step 5: Commit**

```bash
cd /raid/home/a202121010/workspace/projects/ProjectOS && git add src/backend/app/services/hot_context.py src/backend/tests/test_services/test_hot_context.py && git commit -m "feat(hot): render hot context as markdown"
```

---

### Task 3: ObsidianWriterAgent hot.md 생성 통합

**Files:**
- Modify: `src/backend/app/agents/obsidian_writer_agent.py` (`run()` 메서드 + 신규 `_write_hot`)
- Test: `src/backend/tests/test_agents/test_obsidian_writer_agent.py`

- [ ] **Step 1: Write the failing tests**

Append to `src/backend/tests/test_agents/test_obsidian_writer_agent.py` (파일 상단에 이미 `import networkx as nx`, `from app.agents.obsidian_writer_agent import ObsidianWriterAgent`, `tmp_path` fixture 사용 패턴이 있다고 가정; 없으면 추가):

```python
def test_run_writes_hot_md(tmp_path):
    g = nx.DiGraph()
    g.add_node("p1", type="Person", name="Alice", description="ML researcher")
    g.add_node("pr1", type="Project", name="ProjectOS")
    g.add_node("iso", type="Skill", name="Rust")
    g.add_edge("p1", "pr1", relation="DEVELOPED")

    agent = ObsidianWriterAgent()
    agent.run(g, vault_path=str(tmp_path), project_id="proj")

    hot = (tmp_path / "hot.md").read_text(encoding="utf-8")
    assert "# Hot Context — proj" in hot
    assert "[[Alice]]" in hot
    assert "[[Rust]]" in hot  # gap


def test_hot_md_includes_current_build_in_recent(tmp_path):
    g = nx.DiGraph()
    g.add_node("p1", type="Person", name="Alice")
    g.add_node("pr1", type="Project", name="ProjectOS")
    g.add_edge("p1", "pr1", relation="DEVELOPED")

    agent = ObsidianWriterAgent()
    agent.run(g, vault_path=str(tmp_path), project_id="proj")

    hot = (tmp_path / "hot.md").read_text(encoding="utf-8")
    log = (tmp_path / "log.md").read_text(encoding="utf-8")
    # The build event written to log.md must surface in hot.md recent activity
    assert "graph" in log
    assert "## 최근 활동" in hot
    assert "graph" in hot.split("## 최근 활동")[1].split("##")[0]
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/test_agents/test_obsidian_writer_agent.py -k hot -v`
Expected: FAIL — `hot.md` does not exist (`FileNotFoundError`)

- [ ] **Step 3: Write minimal implementation**

In `src/backend/app/agents/obsidian_writer_agent.py`, modify `run()` to call `_write_hot` after `write_payload`. Replace the existing `run()` body's `self.write_payload(...)` call site so it is followed by the hot write. The current `run()` (lines ~131-146):

```python
    def run(
        self,
        graph: nx.DiGraph,
        profiles: list[CareerProfile] | None = None,
        vault_path: str | None = None,
        delta: bool = False,
        project_id: str | None = None,
        progress_callback: Callable[[int, int, str], None] | None = None,
    ):
        payload = self.build_payload(graph, profiles, project_id=project_id, delta=delta)
        self.write_payload(
            payload,
            vault_path=vault_path,
            delta=delta,
            progress_callback=progress_callback,
        )
        self._write_hot(Path(vault_path or config.VAULT_DIR), graph, project_id)
```

Then add the `_write_hot` method (place it right after `run()`):

```python
    def _write_hot(
        self,
        vault: Path,
        graph: nx.DiGraph,
        project_id: str | None,
    ) -> None:
        """Write hot.md: compact session-entry context. Best-effort; never fails the build."""
        try:
            from app.services.hot_context import compose_hot_context, render_hot_markdown
            from app.utils.graph_restructure import (
                build_entity_details,
                demote_project_context_nodes,
            )

            rendered, _ = demote_project_context_nodes(graph.copy())
            rendered, _ = build_entity_details(rendered)

            log_path = vault / "log.md"
            recent_log = (
                log_path.read_text(encoding="utf-8").splitlines()
                if log_path.exists()
                else None
            )
            ctx = compose_hot_context(rendered, project_id, recent_log=recent_log)
            (vault / "hot.md").write_text(render_hot_markdown(ctx), encoding="utf-8")
            logger.info(f"Written: {vault / 'hot.md'}")
        except Exception as exc:  # noqa: BLE001
            logger.warning(f"hot.md generation failed: {exc}")
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/test_agents/test_obsidian_writer_agent.py -v`
Expected: PASS (all existing + 2 new)

- [ ] **Step 5: Commit**

```bash
cd /raid/home/a202121010/workspace/projects/ProjectOS && git add src/backend/app/agents/obsidian_writer_agent.py src/backend/tests/test_agents/test_obsidian_writer_agent.py && git commit -m "feat(hot): write hot.md on vault build"
```

---

### Task 4: projectos_get_hot_context MCP 도구

**Files:**
- Modify: `src/backend/app/mcp_tools.py` (`list_mcp_tools` 등록 + `call_mcp_tool` 핸들러)
- Test: `src/backend/tests/test_api/test_mcp_api.py`

**주의:** `mcp_tools.py`와 `test_mcp_api.py`는 사용자의 미커밋 WIP(simulation/inbox/google)를 포함한다. 이 Task의 구현 subagent는 **git 명령을 실행하지 말 것** — 편집만 남기고 커밋은 하지 않는다. 컨트롤러가 `git show`/`git diff` 없이 `git status`로만 확인하고, 이 변경은 사용자 WIP과 함께 사용자가 커밋하도록 남긴다.

- [ ] **Step 1: Write the failing tests**

Append to `src/backend/tests/test_api/test_mcp_api.py`. 이 파일은 이미 `from fastapi.testclient import TestClient`, `from app.main import app`, `from app.services.project_store import project_store`, 헬퍼 `_write_graph(project_id)`를 갖고 있다. **주의:** project id 속성은 `project.project_id`이고, 각 테스트는 `client = TestClient(app)`를 직접 생성한다(공유 `client` fixture 없음).

```python
def test_mcp_hot_context_tool_registered():
    client = TestClient(app)
    resp = client.post(
        "/mcp",
        json={"jsonrpc": "2.0", "id": 2, "method": "tools/list", "params": {}},
    )
    names = {tool["name"] for tool in resp.json()["result"]["tools"]}
    assert "projectos_get_hot_context" in names


def test_mcp_hot_context_returns_structured():
    project = project_store.create(name="Hot MCP", description="")
    _write_graph(project.project_id)
    client = TestClient(app)
    resp = client.post(
        "/mcp",
        json={
            "jsonrpc": "2.0",
            "id": 4,
            "method": "tools/call",
            "params": {
                "name": "projectos_get_hot_context",
                "arguments": {"project_id": project.project_id},
            },
        },
    )
    result = resp.json()["result"]
    assert result["isError"] is False
    sc = result["structuredContent"]
    assert "persona" in sc
    assert "hubs_by_type" in sc
    assert "stats" in sc


def test_mcp_hot_context_unbuilt_is_error():
    project = project_store.create(name="Hot Unbuilt MCP", description="")
    client = TestClient(app)
    resp = client.post(
        "/mcp",
        json={
            "jsonrpc": "2.0",
            "id": 4,
            "method": "tools/call",
            "params": {
                "name": "projectos_get_hot_context",
                "arguments": {"project_id": project.project_id},
            },
        },
    )
    result = resp.json()["result"]
    assert result["isError"] is True
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/test_api/test_mcp_api.py -k hot_context -v`
Expected: FAIL — tool not registered / `projectos_get_hot_context` 처리 분기 없음 (테스트 함수명에 `hot_context` 포함)

- [ ] **Step 3: Write minimal implementation**

(a) In `src/backend/app/mcp_tools.py`, register the tool inside `list_mcp_tools()` — add this `_tool(...)` entry right after the `projectos_get_graph_health` registration (search for `"projectos_get_graph_health"` in the tool list and insert after its closing `),`):

```python
        _tool(
            "projectos_get_hot_context",
            "Return a compact session-entry primer for a project: key people, hub entities, recent build activity, gaps, and summary stats.",
            {"project_id": {"type": "string"}},
            ["project_id"],
        ),
```

(b) In `call_mcp_tool`, add the handler right after the `projectos_get_graph_health` handler block (search for `if name == "projectos_get_graph_health":`):

```python
        if name == "projectos_get_hot_context":
            from app.services.hot_context import compose_hot_context, render_hot_markdown
            from app.services.vault_reconcile import _rendered_graph

            project_id = str(args["project_id"])
            _require_project(project_id)
            graph = _load_graph(project_id)
            rendered = _rendered_graph(graph)
            log_path = Path(config.VAULT_DIR) / project_id / "log.md"
            recent_log = (
                log_path.read_text(encoding="utf-8").splitlines()
                if log_path.exists()
                else None
            )
            ctx = compose_hot_context(rendered, project_id, recent_log=recent_log)
            return _text_result(render_hot_markdown(ctx), ctx)
```

Note: `_load_graph` already raises `ValueError("Graph not built yet")` for unbuilt projects, and `call_mcp_tool`'s outer `try/except` converts `ValueError` into an `isError` result (verify the existing except block wraps `ValueError` → `_text_result(..., is_error=True)`; mirror whatever `projectos_get_graph_health` relies on).

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/test_api/test_mcp_api.py -k hot_context -v`
Expected: PASS (3 tests)

- [ ] **Step 5: Do NOT commit**

이 Task의 편집(`mcp_tools.py`, `test_mcp_api.py`)은 사용자 WIP과 얽혀 있으므로 커밋하지 않고 작업 트리에 남긴다. `git status`로 변경 파일만 확인.

---

### Task 5: 전체 회귀 검증 및 핸드오프 문서 갱신

**Files:**
- Modify: `docs/claude-code-handoff.md`

- [ ] **Step 1: Run full backend test suite**

Run: `cd /raid/home/a202121010/workspace/projects/ProjectOS/src/backend && python3 -m pytest tests/ -q`
Expected: 모든 테스트 통과(기존 391 + 신규 ~16). 실패 시 해당 Task로 돌아가 수정.

- [ ] **Step 2: Update handoff doc**

`docs/claude-code-handoff.md`에 "#3 Hot cache — 완료" 섹션 추가:
- 변경 내역: `app/services/hot_context.py`(composer+renderer), writer `run()`이 `hot.md` 생성, `projectos_get_hot_context` MCP 도구(미커밋 — 사용자 WIP과 함께 커밋 예정).
- 검증: 백엔드 테스트 전체 통과 카운트 기재.
- 우선순위 목록에서 #3 완료 표시, Last updated를 2026-06-06으로, 다음 작업 = #4 enforced citations.

- [ ] **Step 3: Commit handoff (hot_context core only; MCP wiring stays uncommitted)**

```bash
cd /raid/home/a202121010/workspace/projects/ProjectOS && git add docs/claude-code-handoff.md && git commit -m "docs: handoff update for hot cache (#3)"
```

---

## Self-Review

**Spec coverage:**
- compose_hot_context(persona/hubs/recent/gaps/stats) → Task 1 ✔
- render_hot_markdown → Task 2 ✔
- writer hot.md 생성(빌드 후, log 포함) → Task 3 ✔
- projectos_get_hot_context MCP 도구 → Task 4 ✔
- 렌더 그래프 기준(demote+details), vault_reconcile._rendered_graph 재사용 → Task 3(재유도)/Task 4(import) ✔
- 에러 처리(graph 부재, log 부재, 빈 그래프) → Task 1 test_empty_graph, Task 4 unbuilt test, writer best-effort try/except ✔

**Spec 편차(의도적):** `VaultPayload.rendered_graph` 필드 대신 `run()`에서 렌더 그래프 재유도 — pydantic arbitrary_types 회피. 헤더에 명시.

**Placeholder scan:** 없음. 모든 코드 블록 실제 내용 포함.

**Type consistency:** `compose_hot_context`/`render_hot_markdown` 시그니처가 Task 1·2·3·4 전반에서 일치. 반환 dict 키(persona/hubs_by_type/recent_activity/gaps/stats) 일관.
